[project]
name = "lagdata"
version = "0.1.0"
description = "Lagrummet for developers"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.28",
]
